		<% if( (page == '/index') || (page == '/' ) || (title != '' )) { %>
			<div class="hero-unit">
				<% if( title == '' ) { %>
					<h1 style="padding-bottom:7px!important;">Добро пожаловать!</h1>
					<p>Дабы не заблудиться в этом уютном мини-бложике, 
					мы приготовили для Вас удобную навигацию, интересные и захватывающие темы. 
					Нам есть о чем поговорить с Вами, что обсудить, чем поделиться. 
					Если Вы не страшитесь большого количества новой и интересной информации - 
					добро пожаловать!
					</p>
					<p>
						<a href="#blog" class="btn primary large">Продолжить &raquo;</a>
					</p>
				<% } else { %>
					<h2><%- title %></h2>
				<% } %>
			</div>
		<% } %>
			<% if (words.length > 0) { %>
				<div class="row">
			<% for(var i = 0; i < words.length; i++) { %>
				<% var word = words[i]; %> 
				<div class="span<%- (i % 3 == 0) ? 6 : 5 %>">
					<h2><%- word.title %></h2>
					<p><%- word.description %></p>
					<% if(word.descrs) { %>
						<ol>
							<% word.descrs.forEach(function(descr) { %>
								<li><%- descr %></li>
							<% }); %>
						</ol>
					<% } %>
					<p>
						<a class="btn content" href="#word/<%- word.idnum %>">&#1055;&#1086;&#1076;&#1088;&#1086;&#1073;&#1085;&#1077;&#1077; &raquo;</a>
					</p>
				</div>
			<% } %>
				</div>
			<% } %>		
			<% if (content != '') { %>
				<div class="row">
					<div class="span2">
						<noindex>
							<div class="well">
								<img src="/img/icons/book.png" alt="review" width="24" height="24" />&nbsp;
								<img src="/img/icons/typography.png" alt="review" width="24" height="24" />
							</div>
						</noindex>
					</div>
					<div class="span14">
						<div class="row">
							<div class="span14">
								<%- content %>
							</div>
						</div>
						<div class="row">
							<div class="span3">
								<% if (lastPage != '') { %>
									<a class="btn content" href="<%- lastPage %>">&laquo;&nbsp;Назад</a>
								<% } %>
							</div>
						</div>
					</div>
				</div>
			<% } %>
			<hr>
			<footer>
				<p>&copy; 2011 - <%- year %>, <a href="http://tpg.su/" target="_blank">The Profitware Group</a></p>
			</footer>
			