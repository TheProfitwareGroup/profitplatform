var forever = require('forever');

var child = new (forever.Monitor)(__dirname + '/main-production.js', {
	max: 3,
	silent: false,
	options: []
});

child.on('exit', function(){
	console.log('ProfitPlatform ended by Forever signal');
});
child.start();

forever.startServer(child);
