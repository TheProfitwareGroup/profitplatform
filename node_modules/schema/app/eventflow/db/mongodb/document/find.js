module.exports = exports = function(i, params, result) {
	model = i.model;
    model.where('ref', params.ref).findOne(function(err, obj){
    	if(err||(obj===undefined)){
    		result(err, {});
    	} else {
    		result(null, obj);
    	}
    });
};