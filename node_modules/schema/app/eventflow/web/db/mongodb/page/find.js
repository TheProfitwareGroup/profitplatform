var
    Sync = require('sync');

module.exports = exports = function (i, params) {
    model = i.model;
    var structureList = [],
        obj = undefined;
    try {
        obj = model.find.sync(model);
    } catch (e) {
        obj = [];
    }
    return obj === undefined ? [] : obj;
}.async();